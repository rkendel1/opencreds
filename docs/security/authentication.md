# Authentication

OpenCreds now supports `OPENCREDS_AUTH_MODE` with `anonymous`, `runtime-token`, `jwt`, `proxy`, and `hybrid`.

Identity is derived from an authenticated principal, not directly from raw HTTP identity headers.
